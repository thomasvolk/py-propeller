Changing the time signature has no effect. 

Please change the software to support different time signatures. The following are examples of how to use different time signatures:

- `time_signature=(4, 4)` means that one bar contains 4 beats, and each beat is a quarter note. `C4 * 1` is a quarter note, so `C4 * 4` fills one bar.  
- `time_signature=(8, 8)` means that one bar contains 8 beats, and each beat is an eighth note. `C4 * 1` is a eighth note, so `C4 * 8` fills one bar.
- `time_signature=(4, 8)` means that one bar contains 4 beats, and each beat is an eighth note. `C4 * 1` is a eighth note, so `C4 * 4` fills one bar.
- `time_signature=(8, 4)` means that one bar contains 8 beats, and each beat is a quarter note. `C4 * 1` is a quarter note, so `C4 * 8` fills one bar.
